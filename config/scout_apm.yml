common: &defaults
  monitor: <%= ENV["SCOUT_KEY"].present? %>
  log_level: debug
  key: <%= ENV["SCOUT_KEY"].presence %>
  name: <%= ENV["SCOUT_NAME"].presence || "GrapeTemplate" %>

production:
  <<: *defaults

development:
  <<: *defaults
  monitor: true

test:
  <<: *defaults
  monitor: false

staging:
  <<: *defaults
